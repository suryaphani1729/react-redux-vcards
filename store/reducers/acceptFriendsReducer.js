import {acceptRequests} from '../../models/data';

const initialState = acceptRequests;

const acceptFriendsReducer = (state = initialState, action) => {
  return state;
}

export default acceptFriendsReducer;