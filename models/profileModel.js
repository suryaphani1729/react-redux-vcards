export default class Profile {
  constructor(id, name, email, mobile, address) {
    this.id = id;
         this.name = name;
         this.email = email;
         this.mobile = mobile;
         this.address = address;
  }
}